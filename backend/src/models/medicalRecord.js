const { sequelize, Sequelize } = require('../config/database');

// TODO: Define MedicalRecord model
// const MedicalRecord = sequelize.define('medicalRecord', {}, {});

// module.exports = MedicalRecord;
