const { sequelize, Sequelize } = require('../config/database');

// TODO: Define Prescription model
// const Prescription = sequelize.define('prescription', {}, {});

// module.exports = Prescription;
