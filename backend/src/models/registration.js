const { sequelize, Sequelize } = require('../config/database');

// TODO: Define Registration model
// const Registration = sequelize.define('registration', {}, {});

// module.exports = Registration;
