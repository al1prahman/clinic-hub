const express = require('express');
const router = express.Router();

// TODO: Import controllers
// const { } = require('../controllers/prescription.controller');

// TODO: Define routes
// router.get('/', controller.getAll);

module.exports = router;
